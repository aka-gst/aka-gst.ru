# PsyAdmin — передача состояния, 2 сентября 2026

## Выложено и проверено

- `main` и `origin/main`: `288f941` («Заменить экран PsyAdmin на виджет Orion-S»).
- На `https://aka-gst.ru/psy-admin/` выложены только файлы `psy-admin/`; хеши восьми runtime-файлов совпали с локальными.
- На ширине `390×844`: старого `PSY AI ADMIN · DEMO` нет, закрытая кнопка «Спросить помощника» открывает панель; клуб отвечает `1 000 руб.` и даёт запись; психосоматика не называет цену; кризисный ответ содержит `112` и не содержит ссылок.
- Голосовой путь: микрофон → `SpeechRecognition` ru-RU → текст и синтез речи ответа. Без распознавания есть текстовый fallback. Реальное разрешение микрофона на телефоне не проверялось.

## Не делать без точного источника

Переданная как полный Orion-S папка `/Users/gst/dev/psy-ai-admin/hosted-demo` на текущем `e81f190` — другой старый полноэкранный стенд «AI-администратор для вопросов о центре» с внутренними сценариями, health и review. В нём нет маркеров «Пространство для изменений» или «Найти подходящий формат»; это подтверждено `git log -S` и grep по всем refs. Не заменять им работающий правый виджет.

## Точка входа

Если найдётся точный макет полного Orion-S: перенести только его внешний слой в `psy-admin/index.html` и `psy-admin/styles.css`; оставить `psy-widget.js`, `widget-contract.js`, `router.js`, `content.js` и `intents.js`. Перед выкладкой заново пройти четыре мобильных проверки выше и выложить только `psy-admin/`.
