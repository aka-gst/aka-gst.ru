.DS_Store
node_modules/

# Локальные разрешения Claude Code; launch.json нужен для предпросмотра и хранится.
.claude/settings.local.json
.githooks/private-words.txt

# Локальная копия Pyodide для QA Quest: 13 МБ бинарников. Не версионируется —
# воспроизводится tools/fetch-pyodide.sh из репозитория QA Quest, который
# владеет версией и контрольными суммами. deploy.sh добывает её перед выкладкой.
qa-quest/vendor/
acid/
.shots/
.superpowers/
.playwright-cli/

# Временное: правило 30г. Не хранится и не выкладывается.
trash/

# Рабочие записки и копии страниц заказчика — репозиторий открыт всему миру
# (решение Сергея 6.09.2026: репозиторий остаётся открытым, внутреннее уезжает).
# Записки переехали в /Users/gst/dev/zakrytoe-aka-gst-ru/docs.
# Страницы Орион-С остаются на диске — их шлёт выкладка, — но в открытый
# исходник не попадают; пересобираются из снимка:
#   node psy-admin/tools/build-orion-demo.mjs
docs/
psy-admin/index.html
psy-admin/schedule/index.html
psy-admin/psycluborion/index.html
psy-admin/pweducation/index.html
psy-admin/consultation/index.html
psy-admin/services/index.html
psy-admin/programs/index.html
